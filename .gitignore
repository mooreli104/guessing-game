# Ignore Gradle project-specific cache directory
.gradle
.vscode
docs

# Ignore Gradle build output directory
build

# Ignore Kotlin plugin data
.kotlin

devlog.md
*.class

# Frontend build output, and the copy of it the backend serves. Both are generated --
# the Dockerfile builds the frontend and copies it onto the backend's classpath.
node_modules
frontend/dist
backend/app/src/main/resources/public
